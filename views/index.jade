extends layout

block content
  h1= title
  p Welcome to #{title}
  script(src='/socket.io/socket.io.js')
  script
      var socket = io.connect('http://localhost:3000');
        socket.on('hoge', function (data) {
            console.log(data);
            document.getElementById("fuga").innerHTML = data.hello;
        });


    #fuga